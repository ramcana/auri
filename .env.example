# This is an example file. Copy it to .env and fill in your actual values.

# LLM Service Configuration
LLM_SERVICE_URL=http://localhost:11434/api/chat
LLM_MODEL=mistral
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1024
LLM_TIMEOUT=30

# Session Management
SESSIONS_FILE=sessions.json
SAVE_SESSIONS_INTERVAL=300

# Add any other API keys or configurations here
# For example:
NEWS_API_KEY=your_newsapi_org_key_here
# WEATHER_API_KEY=your_weather_api_key_here # Add your key if using a weather API that requires one. Open-Meteo (default) does not.

# STT Service Configuration
# Full path to the Whisper GGML model file.
# If not set, defaults to stt_service/models/ggml-base.en.bin inside the STT service directory.
# Example: WHISPER_MODEL_PATH=/path/to/your/models/ggml-medium.en.bin
# WHISPER_MODEL_PATH=

# Voice Activity Detection (VAD) Configuration (Optional)
# Controls how VAD trims silence from audio before sending to STT.
# VAD_AGGRESSIVENESS: 0 (least aggressive) to 3 (most aggressive). Default is 3.
# VAD_AGGRESSIVENESS=3
# VAD_FRAME_MS: Frame duration in ms (10, 20, or 30). Default is 30.
# VAD_FRAME_MS=30
