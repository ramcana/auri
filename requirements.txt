# Auri dependencies
# Install with: pip install -r requirements.txt
#
# vLLM is NOT listed here — install separately with CUDA support:
#   pip install vllm
#
# Ollama is a standalone binary — see https://ollama.com/download
#
# Run Auri (Chainlit is single-process by default):
#   chainlit run app.py

chainlit==2.10.0
python-engineio<4.13
openai>=1.35.0,<2.0.0
pyyaml>=6.0
python-dotenv>=1.0
watchdog>=4.0
aiohttp>=3.9
httpx>=0.27

# RAG dependencies
sentence-transformers>=2.7
chromadb>=0.5
pypdf>=4.0           # PDF text extraction

# Web search
duckduckgo-search>=6.0
